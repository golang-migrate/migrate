# options for analysis running
run:
  tests: false

output:
  print-issued-lines: false

linters:
  enable:
    - interfacer
    - unconvert
    - goconst
    - misspell
    - nakedret
    - prealloc
    - scopelint
  disable:
    - errcheck
    - staticcheck